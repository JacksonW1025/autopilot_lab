phase: 1
study_family: aggressive_input_sensitivity
study_layer: manual_whole_loop
study_role: primary
oracle_profile: manual_whole_loop_v1
mode_under_test:
  px4: POSCTL
  ardupilot: STABILIZE
parameter_group: roll_rate_pid
parameter_set_name: baseline
parameter_overrides:
  px4: {}
  ardupilot: {}
controlled_parameters:
  px4:
    - MC_ROLLRATE_P
    - MC_ROLLRATE_I
    - MC_ROLLRATE_D
    - MPC_POS_MODE
    - MPC_ACC_HOR
    - MPC_JERK_MAX
  ardupilot:
    - ATC_RAT_RLL_P
    - ATC_RAT_RLL_I
    - ATC_RAT_RLL_D
    - PSC_VELXY_P
    - LOIT_ACC_MAX
    - LOIT_SPEED
attribution_boundary: 整机闭环 pilot-input sensitivity；不直接归因到 attitude/rate 单层参数。
input_chain: manual
axis: roll
profile_type: step
amplitude: 0.20
bias: 0.0
start_after_s: 2.0
duration_s: 2.0
sample_rate_hz: 20.0
hover_thrust: -0.69
takeoff_thrust: -0.80
takeoff_thrust_max: -0.92
takeoff_ramp_s: 2.0
takeoff_altitude_m: 1.2
takeoff_position_tolerance_m: 0.3
takeoff_velocity_tolerance_m: 0.5
takeoff_settle_s: 0.5
takeoff_timeout_s: 12.0
min_takeoff_clearance_m: 0.35
min_profile_clearance_m: 0.25
altitude_hold_kp: 0.12
altitude_hold_kd: 0.08
thrust_z_min: -0.95
thrust_z_max: -0.35
hold_s: 4.0
timing_required: false
land_timeout_s: 20.0
manual_mode: flight
mode_switch_timeout_s: 5.0
manual_motion_min_displacement_m: 0.20
ardupilot_manual_mode: STABILIZE
ardupilot_manual_throttle_bias: 0.65
ardupilot_manual_throttle_scale: 0.30
